# FreePBX Docker Environment Configuration
# Copy to .env and modify as needed

# Database connection (used by FreePBX container)
DB_HOST=127.0.0.1
DB_USER=freepbxuser
DB_PASS=freepbxpass
DB_NAME=asterisk

# MariaDB settings
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=asterisk
MYSQL_USER=freepbxuser
MYSQL_PASSWORD=freepbxpass
